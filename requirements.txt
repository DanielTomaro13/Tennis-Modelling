# Tennis-Modelling uses the Python standard library only (3.9+).
# There are no third-party runtime dependencies — this file is intentionally
# empty so `pip install -r requirements.txt` is a harmless no-op in CI.
openpyxl>=3.1
